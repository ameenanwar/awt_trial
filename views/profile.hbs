<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0; padding: 0; height: 100vh;
      background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('bg.jpg') no-repeat center center fixed;
      background-size: cover;
      display: flex; justify-content: center; align-items: center;
    }
    .profile-container {
      background: rgba(255, 255, 255, 0.95);
      width: 90%; max-width: 400px;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.3);
      padding: 25px 30px;
    }
    .top-bar { display: flex; justify-content: flex-end; }
    .logout { color: #e74c3c; font-weight: 600; cursor: pointer; text-decoration: none; }
    .logout:hover { text-decoration: underline; }
    .user-info { text-align: center; margin-top: 10px; }
    .avatar { width: 90px; height: 90px; border-radius: 50%; object-fit: cover; border: 3px solid #ddd; margin-bottom: 10px; }
    .user-info h2 { margin: 5px 0; color: #333; }
    .user-info p { color: #555; margin: 0; font-size: 14px; }
    .edit-btn { background-color: #3498db; color: white; border: none; padding: 6px 14px; border-radius: 5px; cursor: pointer; margin-top: 8px; text-decoration: none; display: inline-block; }
    .edit-btn:hover { background-color: #2980b9; }
    .details { margin-top: 20px; font-size: 15px; }
    .detail-item { display: flex; justify-content: space-between; align-items: center; padding: 8px 0; border-bottom: 1px solid #eee; }
    .menu-item { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; font-weight: 500; cursor: pointer; color: #333; }
    .menu-item:hover { color: #3498db; }
    .home-btn { display: block; text-align: center; margin-top: 20px; background: #2ecc71; color: white; text-decoration: none; padding: 8px 0; border-radius: 6px; font-weight: bold; }
    .home-btn:hover { background: #27ae60; }
  </style>
</head>
<body>
  <div class="profile-container">
    <div class="top-bar">
      <a href="/logout" class="logout">Log out</a>
    </div>

    <div class="user-info">
      <img src="profile.jpg" alt="Profile Picture" class="avatar">
      <h2>{{user.username}}</h2>
      <p>{{user.email}}</p>
      <a href="/edit" class="edit-btn">Edit Profile</a>
    </div>

    <div class="details">
      <div class="detail-item">
        <span>📍 Location</span>
        <span>{{user.location}}</span>
      </div>
      <div class="detail-item">
        <span>📞 Mobile No.</span>
        <span>{{user.phone}}</span>
      </div>
    </div>

    <div class="menu">
      <div class="menu-item">Your Orders <span>›</span></div>
      <div class="menu-item">Settings <span>›</span></div>
      <div class="menu-item">About Us <span>›</span></div>
    </div>

    <a href="/home-zm" class="home-btn">🏠 Go to Home</a>
  </div>
</body>
</html>
